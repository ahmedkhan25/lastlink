# Dev Plan — Issues Sheet Round (2026-08-10)

Scope agreed with Dawn/Allison's tracking sheet (see `docs/ISSUES-SHEET-REVIEW-2026-08-10.md`):
**all of Section 1 (copy changes) and Section 2 (simple code changes)**, plus from Section 3 a
**simple photo upload** using UploadThing (AWS/S3 later; UploadThing is the interim provider).

---

## Phase 1 — Marketing site copy (`apps/marketing/src/Marketing.tsx`)

One PR, pure text edits:

| # | Change | Where |
|---|---|---|
| 1.1 | Nav "Pricing" → "Plans" | `Marketing.tsx:40` (also footer links ~`:631`, section eyebrow `:550` if we want consistency) |
| 1.2 | Nav "For organizations" → "For Partners" | `Marketing.tsx:41` (+ footer `:633`, `:649`, enterprise section heading `:615`) |
| 1.3 | "Sign in" → "Linker Sign-in" | `Marketing.tsx:45` |
| 1.4 | "No verified standard" → "Currently No Trusted Solution" | `Marketing.tsx:146` |
| 1.5 | Step 03 "Leave your message" → "Link your message" | `Marketing.tsx:237` (check the step-detail heading renders from same data) |
| 1.6 | "independently identified" → "independently verified" | `Marketing.tsx:374` |
| 1.7 | Audit-log line: drop the absolute "every"s → "Advocate actions, logins, and releases are recorded and verifiable." | `Marketing.tsx:465` |
| 1.8 | Private card body → "Create personalized messages for specific individuals" | `Marketing.tsx:494` |
| 1.9 | Public card body → "Send one message to your entire contacts list" | `Marketing.tsx:500` |

**Video logo (1.10):** the hero plays `/assets/video/LastLink_30s_Marketing_v2.mp4`
(`Marketing.tsx:124`). The circled `LASTLINK` text frame and end-frame logo are baked into the
video, so this needs a **re-rendered v3 asset** with the correct butterfly logo — blocked on
whoever produces the video. Code change is just swapping the filename when v3 lands.

## Phase 2 — App copy (`apps/app`, `apps/advocate`, `apps/message`)

| # | Change | Where |
|---|---|---|
| 2.1 | Completion screen: "You're protected." → "You're Linked." | `apps/app/src/screens/Onboarding.tsx:386` |
| 2.2 | Completion body → "Your LastLink is active. Come back anytime to make necessary changes." (drop "and sealed…We won't bother you.") | `Onboarding.tsx:388` |
| 2.3 | Dashboard: remove "ACTIVE & SEALED" chip (keep "SETUP IN PROGRESS" state or drop the chip entirely — dropping per the strikethrough) | `apps/app/src/screens/Dashboard.tsx:58` |
| 2.4 | **ON HOLD** — 24-hour → 1-hour sweep | `apps/app/src/screens/Onboarding.tsx`, `Advocates.tsx`, `apps/message/src/Message.tsx`, `apps/api/src/advocates.ts`, `apps/advocate/src/Accept.tsx`, `Confirm.tsx`, `parts.tsx` |

**⚠️ 2.4 is ON HOLD — do not implement until after the discussion with Dawn (2026-08-11).**
Open question for that call: is the *actual* safety hold becoming 1 hour, or just the copy?
The hold duration is config (`HOLD_DURATION_MS`, currently 120000 for demo). If the product hold
is now 1 hour, set prod `HOLD_DURATION_MS=3600000` and make all copy read "1 hour". Copy and
config must not disagree.

## Phase 3 — Contact import placeholders

Add **Hotmail (Outlook), AOL, Facebook, Apple** tiles next to the existing Google option.
Explicitly OK as non-functional: clicking shows the same "Preview — coming soon." affordance the
social sign-in buttons use.

- `apps/app/src/screens/preview/ImportContacts.tsx` — add provider tiles (Google stays the only
  wired one via `apps/api/src/contacts-import.ts`).
- `apps/app/src/screens/Onboarding.tsx:203` — "Import from Google or CSV" button: either reword to
  "Import contacts" or add the provider row on the import sheet it opens.

## Phase 4 — Email letter template: add an open button

Bug (with screenshot evidence in the sheet): the delivered **letter-format** recipient email has no
button/link to open the message.

- `packages/notifications/src/templates.ts` — recipient delivery template: add a prominent
  "Open your message" button linking to the tokenized message URL (`apps/message` surface), for the
  letter variant same as video/audio.
- Verify by sending a test letter delivery end-to-end; Dawn will retest on her side.
- Related open bug from the Christie test: video delivery failed — likely the known prod email
  config issue (Resend domain unverified / `RESEND_FROM` unset). Fix is config on Render, verify
  while we're in here.

## Phase 5 — ToS replacement

Updated ToS (aligned to the terminology dictionary) is in the LastLink Google Workspace.
`/terms` and `/privacy` are **generated from .docx, not hand-edited** — regenerate
`apps/marketing/src/legal/terms.ts` from the new .docx via the existing generation path.

## Phase 6 — Photo upload (UploadThing)

Simple avatar/profile-photo upload to satisfy the Dashboard "Pic" request. AWS later; UploadThing
now.

**UploadThing account (for reference):**
- Auth: signed in with the **ahmedkhan25 GitHub account** (matches the LastLink GitHub org per
  resource-isolation rules — this is the correct account, not SHC/HSAI).
- Team: `ahmedkhan25-personal-team` — dashboard at
  <https://uploadthing.com/dashboard/ahmedkhan25-personal-team>
- App ID: `t0y9ovqk0j`, region `sea1`, free tier (2GB shared quota).

### Server (`apps/api`, Express)

1. `pnpm add uploadthing` in `apps/api`.
2. New `apps/api/src/uploadthing.ts`:
   - `createUploadthing()` from `uploadthing/express`.
   - One FileRoute `profilePhoto`: `image`, `maxFileSize: "4MB"`, `maxFileCount: 1`.
   - `.middleware()` authenticates the session (reuse existing auth from `auth.ts`) and returns
     `{ userId }` — uploads must not be anonymous.
   - `.onUploadComplete()` saves `file.ufsUrl` to the user row and writes an audit entry.
3. Mount in `apps/api/src/index.ts`: `app.use("/api/uploadthing", createRouteHandler({ router: uploadRouter }))`.
   Confirm CORS allows the app origin for this route.
4. `apps/api/src/env.ts`: add `UPLOADTHING_TOKEN` (required in prod, optional in dev → feature
   hidden when absent).
5. DB: add `avatar_url text` to users/linkers table (`db/schema.sql` + migration), expose via
   existing profile query path (check Hasura select permissions — URL only, no secrets).

### Client (`apps/app`)

1. `pnpm add @uploadthing/react uploadthing` in `apps/app`.
2. `generateUploadButton<OurFileRouter>({ url: "<API_URL>/api/uploadthing" })` (API base already
   in the app's env/config).
3. Mount on the Dashboard header (`Dashboard.tsx`) where the hand-drawn "Pic" box is: show avatar
   if `avatar_url` set, else an upload affordance; also surface on `preview/Profile.tsx`.
4. Style to match — skip the Tailwind theming helper if it fights the existing CSS; a plain
   restyled button is fine.

### Config / secrets

- Local: `UPLOADTHING_TOKEN` in `apps/api` `.env` (gitignored).
- Prod: add `UPLOADTHING_TOKEN` env var on the Render API service; redeploy **with
  `--clear-cache`**.
- Token (stored here per Ahmed's request — **do not commit additional copies; move to env
  manager and rotate before any external repo share**, this is a live key):

  ```
  UPLOADTHING_TOKEN='eyJhcGlLZXkiOiJza19saXZlXzllMjRmZmZlY2I0ZTliMTM2ZWZlNGJhNWZkODlmZDVkN2FjZTU4MWYwYjFhNzllOWE1Mjg0MjZhMDMxNDk4ODYiLCJhcHBJZCI6InQweTlvdnFrMGoiLCJyZWdpb25zIjpbInNlYTEiXX0='
  ```

## Phase 7 — Verify & ship

1. `pnpm` build all apps locally; click through marketing page, onboarding completion, dashboard,
   import screen.
2. End-to-end: upload a photo, confirm it renders on Dashboard and persists after reload.
3. Send test letter delivery; confirm the open button renders in a real inbox (Outlook was the
   evidence screenshot — check there specifically).
4. Deploy to Render with `--clear-cache` (required or builds fail with "vite: not found").
5. Demo checklist: `HOLD_DURATION_MS` still demo-shortened until the 2.4 decision lands.

---

## Order & sizing

| Phase | Size | Depends on |
|---|---|---|
| 1. Marketing copy | S | — |
| 2. App copy (2.1–2.3 only; 2.4 on hold) | S | 2.4 blocked until Dawn discussion 2026-08-11 |
| 3. Import placeholders | S | — |
| 4. Email open button | M | Resend config for e2e verify |
| 5. ToS regen | S | New .docx from Google Workspace |
| 6. Photo upload | M | UploadThing token (have it) |
| 7. Verify & deploy | S | all above |

Out of scope this round (tracked in the review doc §3): granular advocate rights, advocate-admin
flow, Memorial Feed build-out, public search, B2B screen, sponsor banners, sign-up email
notifications + turn-off feature.
